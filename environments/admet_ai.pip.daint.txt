# ADMET-AI for Daint (aarch64/GH200). CPU-only tool — no EDF, no container.
# admet-ai is pinned below 2.0: the 2.x line requires chemprop 2.x, which pulls
# cuik_molmaker_pin and lightning, neither of which publishes a linux-aarch64
# wheel. 1.4.0 is the newest release supporting the host python 3.11, and its
# hard torch==2.5.0 pin has a cp311 manylinux2014_aarch64 wheel.
admet-ai<2.0
# setuptools<81 needed: chemprop 1.6.1 -> hyperopt 0.2.7 imports pkg_resources,
# which setuptools removed in 81+.
setuptools<81
