# outputs

This folder is for storing output for locally executed pipelines (i.e. when a
`Pipeline(..., local_output=True)` writes into the current working directory
instead of the site-configured output path).

- **Not committed**: Your files here are gitignored (except this README).
